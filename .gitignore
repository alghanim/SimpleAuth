# Binary (root build output only — anchored so it doesn't match the
# sdk/python/simpleauth/ package or other paths named "simpleauth")
/simpleauth

# Build output
/dist/
data/
# Data directory
data/

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# Node (UI build + SDK)
node_modules/
package-lock.json
sdk/js/dist/

# Config (auto-generated on first run)
simpleauth.yaml
simpleauth.yml

# Environment
.env
encrypt.key

# Secrets / at-rest data key (never commit) and generated key material
secret.key
**/secret.key
*.keytab
private.pem
public.pem

# Python
__pycache__/
*.py[cod]
sdk/python/dist/
sdk/python/*.egg-info/
